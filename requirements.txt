# Multi-Agent Bias Detection System — Python Dependencies
# Requires Python 3.10+
#
# Install with:
#   pip install -r requirements.txt
#
# Then start your vLLM servers (see README.md or docs/MODELS.md for commands).

# ── Inference client ──────────────────────────────────────────────────────────
# Communicates with vLLM servers via the OpenAI-compatible API.
openai>=1.30.0

# ── Data processing ───────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
openpyxl>=3.1.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
python-dateutil>=2.8.2
jsonlines>=3.1.0

# ── vLLM (install on the GPU server, not the client machine) ─────────────────
# The inference servers run vLLM separately. Install it on the machine that
# holds the GPUs:
#   pip install vllm
# See docs/MODELS.md for per-model server startup commands.
